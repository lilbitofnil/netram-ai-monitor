import { createFileRoute } from "@tanstack/react-router";import { Video,VideoOff,Activity } from "lucide-react";import { SectionHeader,StatCard,StatusBadge } from "@/components/netram/ui";import { useAdminOverview } from "@/lib/useAdminData";export const Route=createFileRoute("/admin/cctv")({head:()=>({meta:[{title:"CCTV Monitoring — NETRAM AI"},{name:"description",content:"CCTV configuration and connectivity status."},{property:"og:title",content:"CCTV Monitoring — NETRAM AI"},{property:"og:description",content:"CCTV configuration and connectivity status."}]}),component:Page});function Page(){const {data}=useAdminOverview();const rows=data?.ngos??[];return <div className="mx-auto max-w-6xl"><SectionHeader title="CCTV Monitoring" subtitle="Integration readiness and device connectivity"/><div className="grid gap-4 sm:grid-cols-3"><StatCard icon={Video} label="Total facilities" value={rows.length}/><StatCard icon={Activity} label="Active" value={rows.filter(n=>n.cctv_status==="active").length} accent="success"/><StatCard icon={VideoOff} label="Offline" value={rows.filter(n=>n.cctv_status!=="active").length} accent="destructive"/></div><div className="mt-5 grid gap-4 md:grid-cols-2">{rows.map(n=><article key={n.id} className="surface p-5"><div className="flex items-center justify-between"><h2 className="font-bold">{n.name}</h2><StatusBadge label={n.cctv_status} tone={n.cctv_status==="active"?"success":"danger"}/></div><div className="mt-6 rounded-xl border border-dashed border-border bg-muted/40 py-12 text-center"><VideoOff className="mx-auto h-7 w-7 text-muted-foreground"/><p className="mt-3 text-sm font-bold">CCTV STREAM NOT CONFIGURED</p><p className="mt-1 text-xs text-muted-foreground">Connection controls will activate after a secure stream is added.</p></div></article>)}</div></div>}
