import { createFileRoute, Link, Outlet, useRouterState } from "@tanstack/react-router";
import { Home, ClipboardCheck, UserRound, LogOut } from "lucide-react";
import { NetramLogo } from "@/components/netram/ui";
import { supabase } from "@/integrations/supabase/client";
export const Route=createFileRoute("/_authenticated/officer")({component:Layout});
function Layout(){const path=useRouterState({select:s=>s.location.pathname});const nav=[{to:"/officer",label:"Home",icon:Home},{to:"/officer/inspection",label:"Inspect",icon:ClipboardCheck},{to:"/officer/profile",label:"Profile",icon:UserRound}] as const;return <div className="min-h-screen bg-background pb-20 md:pb-0"><header className="sticky top-0 z-20 border-b border-border bg-card"><div className="mx-auto flex max-w-6xl items-center justify-between px-5 py-3.5"><NetramLogo/><div className="hidden items-center gap-2 md:flex">{nav.map(n=><Link key={n.to} to={n.to} className={`flex items-center gap-2 rounded-xl px-3 py-2 text-sm font-semibold ${path===n.to?"bg-primary/10 text-primary":"text-muted-foreground hover:bg-muted"}`}><n.icon className="h-4 w-4"/>{n.label}</Link>)}<button aria-label="Sign out" title="Sign out" onClick={()=>supabase.auth.signOut()} className="rounded-xl p-2 text-muted-foreground hover:bg-muted hover:text-destructive"><LogOut className="h-5 w-5"/></button></div></div></header><main className="mx-auto max-w-6xl px-5 py-6"><Outlet/></main><nav className="fixed inset-x-0 bottom-0 z-20 flex border-t border-border bg-card md:hidden">{nav.map(n=><Link key={n.to} to={n.to} className={`flex flex-1 flex-col items-center gap-1 py-2.5 text-[11px] font-semibold ${path===n.to?"text-primary":"text-muted-foreground"}`}><n.icon className="h-5 w-5"/>{n.label}</Link>)}</nav></div>}
